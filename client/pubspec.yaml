name: pocketclaude
description: Mobile remote AI coding tool - control Claude Code from your pocket
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^2.5.0
  riverpod_annotation: ^2.3.0

  # WebSocket
  web_socket_channel: ^2.4.0

  # Crypto
  cryptography: ^2.7.0
  pointycastle: ^3.7.0

  # QR Code
  mobile_scanner: ^4.0.0
  qr_flutter: ^4.1.0

  # UI
  flutter_markdown: ^0.7.0
  google_fonts: ^6.1.0
  flutter_highlight: ^0.7.0

  # Navigation
  go_router: ^14.0.0

  # Storage
  shared_preferences: ^2.2.0
  flutter_secure_storage: ^9.0.0

  # Utils
  uuid: ^4.3.0
  json_annotation: ^4.8.0
  freezed_annotation: ^2.4.0
  intl: ^0.19.0
  url_launcher: ^6.2.0
  connectivity_plus: ^6.0.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.0
  freezed: ^2.4.0
  json_serializable: ^6.7.0
  riverpod_generator: ^2.3.0

flutter:
  uses-material-design: true
